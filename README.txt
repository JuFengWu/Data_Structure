This is data structure examples which is written by c++

### Attention: this code is writed in visual studio 2017, older version may not compatible
---
In sort folder, it demostrate two sort, one is merge sort, other is heap sort.

---
In sort.h , you can undefine "CPP17CODE" to switch to cpp 17.
But current google test can't support cpp17, there will be some errors when build test project.

---
In project "SortTest", I use google test to test the function. 
You can click "test->run->all test" to run the test function.